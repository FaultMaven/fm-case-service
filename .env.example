# FaultMaven Case Service - Environment Configuration

# ============================================================================
# Storage Configuration (Pluggable Backends)
# ============================================================================
# Case storage: inmemory (dev/testing) | postgres (production)
CASE_STORAGE_TYPE=inmemory

# PostgreSQL connection (when CASE_STORAGE_TYPE=postgres)
DATABASE_URL=postgresql+asyncpg://faultmaven:password@localhost:5432/faultmaven_cases

# ============================================================================
# Application Settings
# ============================================================================
PORT=8003
LOG_LEVEL=INFO
ENVIRONMENT=development
